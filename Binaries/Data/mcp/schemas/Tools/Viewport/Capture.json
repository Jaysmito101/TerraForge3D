{
  "$include": "Common/Object.json",
  "properties": {
    "ViewportId": { "$include": "Common/ViewportId.json" },
    "MaxDimension": {
      "type": "integer",
      "minimum": 1,
      "description": "Optional maximum width or height in pixels. Use this to reduce image size and token usage."
    }
  },
  "required": ["ViewportId"]
}
